name: PR Review with Vault Secrets

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

permissions:
  contents: read
  pull-requests: write
  id-token: write # 必须开启 OIDC

jobs:
  vault-pr-review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      # --- Step 1: Login to Vault using GitHub OIDC ---
      - name: Login to Vault
        uses: hashicorp/vault-action@v2
        with:
          method: jwt
          path: jwt-github # Vault auth mount
          role: github-action-role # Vault role
          url: https://cca-public-vault-85315103.c755c6be.z1.hashicorp.cloud:8200
          namespace: admin # Vault namespace
          jwtGithubAudience: vault # 必须和 Vault role bound_audiences 一致
          exportToken: true # 导出 VAULT_TOKEN 给后续 steps

      # --- Step 2: Retrieve secrets from Vault ---
      - name: Get Vault secrets
        uses: hashicorp/vault-action@v2
        with:
          method: token
          path: kv # KV mount 名称
          namespace: admin
          url: https://cca-public-vault-85315103.c755c6be.z1.hashicorp.cloud:8200
          secrets: |
            kv/anthropic_api_key value | ANTHROPIC_API_KEY
            kv/anthropic_base_url value | ANTHROPIC_BASE_URL

      # --- Step 3: Run Claude PR review ---
      - name: PR Review with Progress Tracking
        uses: anthropics/claude-code-action@v1
        with:
          track_progress: true
          anthropic_api_key: ${{ env.ANTHROPIC_API_KEY }}
          prompt: |
            REPO: ${{ github.repository }}
            PR NUMBER: ${{ github.event.pull_request.number }}

            Perform a comprehensive code review with the following focus areas:

            1. **Code Quality**
               - Clean code principles and best practices
               - Proper error handling and edge cases
               - Code readability and maintainability

            2. **Security**
               - Check for potential security vulnerabilities
               - Validate input sanitization
               - Review authentication/authorization logic

            3. **Performance**
               - Identify potential performance bottlenecks
               - Review database queries for efficiency
               - Check for memory leaks or resource issues

            4. **Testing**
               - Verify adequate test coverage
               - Review test quality and edge cases
               - Check for missing test scenarios

            5. **Documentation**
               - Ensure code is properly documented
               - Verify README updates for new features
               - Check API documentation accuracy

            Provide detailed feedback using inline comments for specific issues.
            Use top-level comments for general observations or praise.

          # Tools for comprehensive PR review
          claude_args: |
            --allowedTools "mcp__github_inline_comment__create_inline_comment,Bash(gh pr comment:*),Bash(gh pr diff:*),Bash(gh pr view:*)"
        env:
          ANTHROPIC_API_KEY: ${{ env.ANTHROPIC_API_KEY }}
          ANTHROPIC_BASE_URL: ${{ env.ANTHROPIC_BASE_URL }}
